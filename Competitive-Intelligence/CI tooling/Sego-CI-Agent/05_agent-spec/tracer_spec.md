# Tracer Agent — Build Spec (TBD)

The CI research agent, named **Tracer** ("one who tracks"). Culminating deliverable. Must encode: three-track monthly assembly (brief §2.5), tracker schema (§2.6), the two retrieval routines (tracker refresh vs standalone non-asset searches), the storage decision (open thread 1: prototype in Excel, migrate to SQLite before Tracer's read/write code is written), the dedup rule (open thread 4), and the per-indication source checklist (open thread 3 / see 06_sources).
